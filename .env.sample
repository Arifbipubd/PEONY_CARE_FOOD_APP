# Copy this file to .env and fill in the values.
# Never commit .env — it is git-ignored.

# Base URL of the Peony Care Django REST API (no trailing slash)
EXPO_PUBLIC_API_URL=http://your-server-ip:8000/api/v1
